name with fields id(primary key) and name

name_id with fields id(primary key), id and name_id(foreign key)from name table

model with fields id(primary key), name and nameid_id(foreign key) from name_id table.
